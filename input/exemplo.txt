/*
 * Teste de Validação Final (Pós-Refatoração Completa)
 *
 * Valida:
 * 1. Lexer, Parser, Declarações
 * 2. Refatoração do Visitor<R>
 * 3. Refatoração do LiteralExpression (para Números, Floats, Strings, Bools)
 * 4. Refatoração SOLID (Checagem de tipo no AssignStatement do Visitor)
 * 5. Coerção de tipo (int + float)
 * 6. Lógica (while, if, &&)
 * 7. Concatenação de String
 */

// Declaração de todos os tipos
int a;
float b;
bool c;
string s;
int i; // <-- CORREÇÃO: Linha movida da linha 40 para cá

// Teste de Literais (LiteralExpression)
a = 10;
b = 1.5;
c = true;
s = "Hello";

// Teste de Concatenação de String e Print
print s + " World!"; // Esperado: Hello World!

// Teste de coerção em atribuição (SOLID fix)
b = a; // b (float) = a (int 10)
print "Teste de Coercao (Atribuicao):";
print b; // Esperado: 10.0

// Teste de coerção aritmética (BinaryExpression)
b = b + 0.5; // b (10.0) + 0.5
print "Teste de Coercao (Aritmetica):";
print b; // Esperado: 10.5

// Teste de Loop (while) e Lógica (if/else)
i = 0; // A declaração 'int i;' já foi feita no topo

while (i < 3) {
    if (c == true && i < 2) {
        print "Loop (i < 2):";
        print i;
    } else {
        print "Loop (else):";
        print i;
    }
    i = i + 1;
}

print "Teste Final (String + Int):";
print s + i; // Esperado: Hello3